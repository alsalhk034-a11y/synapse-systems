name: Deploy to Cloudflare Workers

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Type check (verbose)
        run: |
          echo "=== tsc -b --verbose output ==="
          npx tsc -b --verbose 2>&1 | tee tsc-output.log
          echo "=== END ==="
        continue-on-error: true

      - name: Upload tsc logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tsc-logs
          path: tsc-output.log
          if-no-files-found: ignore

      - name: Build (Vite) - with debug
        run: |
          echo "=== Environment ==="
          node --version
          npm --version
          echo "=== Starting build ==="
          npm run build 2>&1 | tee build-output.log
          echo "=== Build exit code: $? ==="
        env:
          NODE_ENV: production

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build-output.log
          if-no-files-found: ignore

      - name: List dist directory
        run: |
          echo "=== dist/ contents ==="
          ls -la dist/ 2>&1 || echo "no dist dir"
          echo "=== assets/ contents ==="
          ls -la dist/assets/ 2>&1 || echo "no assets"

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./
          command: deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
